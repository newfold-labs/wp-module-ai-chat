/**
 * Input Component Configuration
 * Constants for chat input behavior (used by ChatInput).
 */
export const INPUT = {
	MAX_HEIGHT: 200, // Textarea max height (px)
	FOCUS_DELAY: 100, // Delay before focusing input (ms)
	STOP_DEBOUNCE: 500, // Debounce for stop button (ms)
};
